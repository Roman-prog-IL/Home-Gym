<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Gym OS v2.0</title>
    <style>
        body { background-color: #0f172a; color: white; font-family: -apple-system, sans-serif; display: flex; justify-content: center; margin: 0; padding: 20px; }
        .container { width: 100%; max-width: 400px; }
        header { border-bottom: 1px solid #1e293b; padding-bottom: 15px; margin-bottom: 20px; }
        header h1 { font-size: 28px; margin: 0; color: #3b82f6; }
        #date-box { color: #94a3b8; font-size: 14px; }
        .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
        .card { background: #1e293b; padding: 20px; border-radius: 20px; border-left: 5px solid; display: flex; flex-direction: column; }
        .blue { border-color: #3b82f6; } .orange { border-color: #f97316; }
        .yellow { border-color: #eab308; } .green { border-color: #22c55e; }
        .val { font-size: 32px; font-weight: bold; }
        .lab { font-size: 11px; color: #94a3b8; font-weight: bold; margin-top: 5px; }
        .actions { background: #1e293b; padding: 15px; border-radius: 20px; margin-top: 20px; display: flex; justify-content: space-between; align-items: center; border: 1px solid #334155; }
        button { background: #334155; color: #3b82f6; border: none; padding: 10px 15px; border-radius: 10px; font-weight: bold; cursor: pointer; }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1 id="day-name">Загрузка...</h1>
            <div id="date-box">Синхронизация с Израилем...</div>
        </header>

        <div class="grid">
            <div class="card blue"><span class="val">0</span><span class="lab">БРУСЬЯ</span></div>
            <div class="card orange"><span class="val">0</span><span class="lab">ТУРНИК</span></div>
            <div class="card yellow"><span class="val">0.0</span><span class="lab">ДОРОЖКА</span></div>
            <div class="card green"><span class="val">0</span><span class="lab">ПРИСЕДЫ</span></div>
        </div>

        <div class="actions">
            <span>💧 ВОДА: <strong id="water-val">0.0</strong>л</span>
            <div>
                <button onclick="addWater(0.1)">+0.1</button>
                <button onclick="addWater(0.3)">+0.3</button>
            </div>
        </div>
        
        <p style="text-align:center; color:#334155; font-size:10px; margin-top:30px;">v2.0 - Силовая А готова</p>
    </div>

    <script>
        let water = 0.0;
        function addWater(val) {
            water = Math.round((water + val) * 10) / 10;
            document.getElementById('water-val').innerText = water.toFixed(1);
        }

        const days = ['Воскресенье', 'Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота'];
        const months = ['января', 'февраля', 'марта', 'апреля', 'мая', 'июня', 'июля', 'августа', 'сентября', 'октября', 'ноября', 'декабря'];
        const now = new Date();
        document.getElementById('day-name').innerText = days[now.getDay()];
        document.getElementById('date-box').innerText = now.getDate() + ' ' + months[now.getMonth()] + ' ' + now.getFullYear() + ' г.';
    </script>
</body>
</html>
